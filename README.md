the visual basic files are meant to take in a list of comma-separated websites and search through a specific excel sheet to output one sheet per website in a client friendly way

the js file is meant to be a bookmarklet that can work around bad UI to let support agents open multiple links instead of having to manually search for all of them